import { pageTitle } from './pageTitle'
import { menu } from './menu'
import { common } from './common'
import { video } from './video'

export const vi = {
  pageTitle,
  menu,
  common,
  video,
  // Các phần khác sẽ được thêm vào sau
}
